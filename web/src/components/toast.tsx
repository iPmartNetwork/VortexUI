// Unified toast system — re-exports from the enhanced ToastProvider.
// The old `useToast` name is preserved so existing consumers don't break.
export { useToastV2 as useToast, ToastProviderV2 as ToastProvider } from "./ToastProvider";
